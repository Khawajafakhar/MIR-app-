import 'package:flutter/material.dart';

class MainButtonWidet extends StatelessWidget {
  const MainButtonWidet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}